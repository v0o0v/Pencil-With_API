package com.pencilwith.apiserver.working.mapper;

public class ProjectTitleMapper {
//    public static List<projectServiceDTO> toDto(List<Project> projects) {
//        return projects.stream().map(project -> projectServiceDTO.builder()
//                                .projectId(project.getId())
//                                .title(project.getTitle())
//        .build()).collect(Collectors.toList());
//    }
}
